def count_vowels(string):
    """
    Функция считает количество гласных в строке.
    Гласные: a, e, i, o, u, а, е, ё, и, о, у, ы, э, ю, я.
    """
    vowels = 'aeiouAEIOUаеёиоуыэюяАЕЁИОУЫЭЮЯ'
    count = sum(1 for char in string if char in vowels)
    return count